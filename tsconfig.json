{
  "compilerOptions": {
    "target": "ESNext", // Change to your target ECMAScript version
    "module": "CommonJS", // Change to your preferred module system
    "outDir": "./dist", // Specify the output directory
    "rootDir": "./src", // Specify the source directory
    "strict": true,
    "esModuleInterop": true
    // Add any other compiler options you need
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
